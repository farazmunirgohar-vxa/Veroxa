kind = "web"
previewPath = "/"
title = "Veroxa Growth OS"
version = "1.0.0"
id = "artifacts/veroxa"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 24693

[services.development]
run = "pnpm --filter @workspace/veroxa run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/veroxa", "run", "build" ]
publicDir = "artifacts/veroxa/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "24693"
BASE_PATH = "/"
