# R3 Pre-Intervention Authority — 2026-08-20

This is the controlling written authority for the Veroxa R3 pre-intervention
program. It supersedes conflicting “current” claims in older date-stamped
documents until R3-02 records a new fully verified checkpoint.

## Authority order

1. Live verified GitHub, Sites, Supabase, and Linear evidence.
2. The R3 program in Linear: VER-20 through VER-28, in dependency order.
3. Exact reviewed source, workflow, and recorded evidence.
4. Earlier documents as historical context only.

Do not infer a production claim from a candidate branch, saved Sites version,
repository file, or old release record.

## Locked product and safety boundary

- IMG_4257 is immutable, terminal, read-only, non-Ready, and has zero future
  retries. Do not process, retry, move, delete, re-upload, replace, or use it
  as a fixture.
- `ready_for_team_review` is an internal evidence state, not Team approval,
  scheduling, publication, or an external action.
- Momo remains the free founding pilot.
- Publishing, provider writes, review replies, messaging, website/listing/menu
  writes, advertising, external scheduling, customer contact, and repository
  visibility changes remain fail-closed unless separately and explicitly
  authorized.

## Baseline that must not be overstated

- GitHub `main` is the post-PR-191 state. PR #193 is the current acceptance
  candidate only; it has unresolved review requirements and failing CI and
  Supabase verification, so it is not production authority. PR #187 remains
  deferred and unmerged.
- Sites version 60 is saved, but this checkpoint has not proven its deployment
  identity. Version 59 is the last deployment asserted by prior evidence and
  must be reverified; neither version may be treated as an unqualified current
  production claim.
- The remote Supabase ledger contains 59 applied migrations. The acceptance
  migration remains a candidate until the R3 release sequence proves and
  applies it. The July readiness-gate result is stale evidence, not a current
  readiness decision.
- OpenAI integration must be mapped from non-secret configuration presence,
  runtime policy, and executed evidence only. Credentials must not be read,
  exposed, or recorded.

## Required R3 order

1. VER-20: authority freeze and documentation correction.
2. VER-21: exact live baseline and discrepancy classification.
3. VER-22 through VER-27: only the ordered code, CI, security, convergence,
   synthetic-proof, and authenticated-portal gates.
4. VER-28: final simultaneous gate; GO is permitted only when every stated
   gate is verified on the same current release.

No R3 task may independently authorize a migration, deployment, real Momo
media operation, external provider use, or customer-facing action. If a gate is
unproven, preserve the exact checkpoint and identify the smallest safe next
repair or approval.

## Document status

AGENTS.md, this document, the current-state index, and CURRENT_MILESTONE.md
form the active written authority chain. CURRENT_STATE.json, deployment
manifests, and earlier date-stamped release sections remain evidence inputs and
historical records until their exact claims are revalidated by R3-02.
