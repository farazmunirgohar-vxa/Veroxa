import { AUTH_MODE } from "@/lib/auth/authMode";
import { MOMO_AI_DRAFT_GENERATION_FEATURE_FLAG, isMomoAiDraftGenerationFeatureEnabled, type MomoAiDraftGenerationStatus } from "./momoAiDraftGenerationRules";
export type MomoAiDraftGenerationRequest = { contentGoal: string; contentPillar: string; confirmedBusinessFacts: string[]; draftType: string; intendedInternalReviewer: "team"; mediaReference?: { id: string; rightsConfirmed: boolean }; blockedClaims: string[]; brandRulesReviewed: boolean; publicationDestination?: never };
export type MomoAiDraftGenerationPreflight = { eligible: boolean; status: MomoAiDraftGenerationStatus; reasons: string[]; featureFlag: typeof MOMO_AI_DRAFT_GENERATION_FEATURE_FLAG };
export function preflightMomoAiDraftGeneration(request?: Partial<MomoAiDraftGenerationRequest>): MomoAiDraftGenerationPreflight { const reasons: string[] = []; if (AUTH_MODE !== "real") reasons.push("AUTH_MODE is placeholder; real auth is required before any future AI generation."); if (!isMomoAiDraftGenerationFeatureEnabled()) reasons.push(`${MOMO_AI_DRAFT_GENERATION_FEATURE_FLAG} is off by default.`); if (!request?.confirmedBusinessFacts?.length) reasons.push("Confirmed business truth is required."); if (request?.mediaReference && !request.mediaReference.rightsConfirmed) reasons.push("Media-rights confirmation is required before media may be referenced."); if (!request?.brandRulesReviewed) reasons.push("Brand voice and prompt rules must be reviewed."); return { eligible: false, status: reasons.length ? "disabled_by_default" : "future_step_required", reasons: reasons.length ? reasons : ["Provider implementation is intentionally absent in PR #118."], featureFlag: MOMO_AI_DRAFT_GENERATION_FEATURE_FLAG }; }
