import { RealRoutePlaceholder } from "@/pages/real-route-placeholder";

export default function RealOwnerPlaceholder() {
  return (
    <RealRoutePlaceholder
      role="owner"
      area="Portal"
      futurePath="/owner/dashboard"
      demoPath="/demo/owner/dashboard"
    />
  );
}
