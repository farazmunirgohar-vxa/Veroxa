import { AlertTriangle, ClipboardList, Lock, ShieldAlert } from "lucide-react";
import { Link } from "wouter";
import { PortalLayout } from "@/components/PortalLayout";
import { TeamReviewModeRouteSummary } from "@/components/team/TeamOperationalSpine";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";
import { MOMO_AI_DRAFT_ALLOWED_TEAM_LINKS, MOMO_AI_DRAFT_GENERATION_CATEGORIES, MOMO_AI_DRAFT_GENERATION_RULE_ITEMS, MOMO_AI_DRAFT_OUTPUT_SAFETY_LABELS, type MomoAiDraftGenerationRuleItem } from "@/lib/momoAiDraftGeneration/momoAiDraftGenerationRules";
import { teamPortalNavItems } from "@/lib/teamPortalNav";

const requiredCopy = ["Controlled AI draft generation foundation only.", "AI generation is disabled by default.", "This does not generate customer-visible AI output.", "This does not auto-approve AI output.", "This does not publish AI output.", "This does not activate the pilot.", "This does not turn on real auth.", "This does not create credentials.", "This does not contact Momo’s House.", "This does not upload, create, seed, generate, or fake media.", "This does not publish externally.", "This does not connect Google, Meta, Yelp, TikTok, or delivery platforms.", "AI may use only confirmed business truth and permissioned media in later internal drafts.", "Business-truth changes still require owner confirmation.", "Media usage rights require owner confirmation before public use.", "Sensitive claims are blocked until owner-confirmed.", "All future AI output requires Team/Faraz review before customer-visible use.", "Momo owner walkthrough remains blocked.", "No next activation PR is approved by default.", "Future real-world activation requires separate explicit Faraz approval."];
const sectionLabels: Record<string, string> = { "Generation Gate": "Generation gate", "Required Inputs": "Required inputs", "Business Truth Dependencies": "Business truth dependencies", "Media Rights Dependencies": "Media rights dependencies", "Brand Voice Dependencies": "Brand voice dependencies", "Prompt Assembly Rules": "Prompt assembly rules", "Blocked Claims": "Blocked claims", "Allowed Draft Types": "Allowed draft types", "Output Safety Labels": "Output safety labels", "Team Review Workflow": "Team review workflow", "Disabled / Failure States": "Disabled / failure states", "Future Enablement Requirements": "Future enablement requirements", "Safe Internal Next Decision": "Safe internal next decision" };
function RuleCard({ item }: { item: MomoAiDraftGenerationRuleItem }) { return <Card><CardContent className="p-4"><div className="flex flex-wrap items-center gap-2"><Badge variant="secondary">{item.category}</Badge><Badge variant={item.risk === "critical" ? "destructive" : "outline"}>{item.status.replaceAll("_", " ")}</Badge><Badge variant={item.risk === "critical" ? "destructive" : "outline"}>{item.risk}</Badge></div><h3 className="mt-2 font-semibold">{item.title}</h3><p className="mt-1 text-sm text-muted-foreground">{item.description}</p><p className="mt-2 text-xs text-muted-foreground"><strong>Allowed behavior:</strong> {item.allowed_behavior}</p><p className="mt-1 text-xs text-muted-foreground"><strong>Blocked behavior:</strong> {item.blocked_behavior}</p><p className="mt-1 text-xs text-muted-foreground"><strong>Required confirmation:</strong> {item.required_confirmation}</p><p className="mt-1 text-xs text-muted-foreground"><strong>Safe internal next step:</strong> {item.safe_internal_next_step}</p>{item.route_href ? <Button asChild className="mt-3" size="sm" variant="outline"><Link href={item.route_href}>Open existing Team page</Link></Button> : null}</CardContent></Card>; }
export default function TeamMomoAiGeneration() { const blockers = MOMO_AI_DRAFT_GENERATION_RULE_ITEMS.filter((item) => item.risk === "critical" || item.status === "disabled_by_default" || item.status === "blocked"); return <PortalLayout items={teamPortalNavItems} portalName="Team Portal"><div className="mb-4 flex items-start justify-between gap-3"><div><h2 className="text-2xl font-bold tracking-tight" data-testid="header-team-momo-ai-generation">Momo Controlled AI Draft Generation Foundation</h2><p className="mt-1 text-sm text-muted-foreground">Team-only internal architecture for future safe draft generation. No generation, publishing, owner contact, credentials, media creation, or activation happens here.</p></div><Badge variant="outline">Team only</Badge></div><Card className="border-amber-500/30 bg-amber-500/10"><CardContent className="grid gap-1 p-4 text-sm text-amber-100">{requiredCopy.map((line) => <p key={line}><ShieldAlert className="mr-2 inline h-4 w-4" />{line}</p>)}</CardContent></Card><section className="mt-4 grid gap-3 md:grid-cols-3"><Card><CardContent className="p-4"><p className="text-xs text-muted-foreground">Overall AI generation status</p><p className="mt-2 text-2xl font-bold">Disabled by default</p><p className="mt-1 text-xs text-muted-foreground">Placeholder mode remains disabled; the feature flag defaults off.</p></CardContent></Card><Card><CardContent className="p-4"><p className="text-xs text-muted-foreground">Critical generation blockers</p><p className="mt-2 text-2xl font-bold">{blockers.length}</p><p className="mt-1 text-xs text-muted-foreground">Real auth, Team role, feature flag, confirmed truth, media rights, brand review, and Team review remain blockers.</p></CardContent></Card><Card><CardContent className="p-4"><p className="text-xs text-muted-foreground">Output labels required later</p><p className="mt-2 text-2xl font-bold">{MOMO_AI_DRAFT_OUTPUT_SAFETY_LABELS.length}</p><p className="mt-1 text-xs text-muted-foreground">Internal-only, review-gated, not customer-visible, and not published.</p></CardContent></Card></section><Card className="mt-4"><CardHeader><CardTitle className="flex items-center gap-2 text-sm"><AlertTriangle className="h-4 w-4" />Critical generation blockers</CardTitle></CardHeader><CardContent className="grid gap-3">{blockers.map((item) => <RuleCard key={item.id} item={item} />)}</CardContent></Card>{MOMO_AI_DRAFT_GENERATION_CATEGORIES.map((category) => <Card className="mt-4" key={category}><CardHeader><CardTitle className="flex items-center gap-2 text-sm"><ClipboardList className="h-4 w-4" />{sectionLabels[category]}</CardTitle></CardHeader><CardContent className="grid gap-3">{MOMO_AI_DRAFT_GENERATION_RULE_ITEMS.filter((item) => item.category === category).map((item) => <RuleCard key={item.id} item={item} />)}</CardContent></Card>)}<Card className="mt-4"><CardHeader><CardTitle className="flex items-center gap-2 text-sm"><Lock className="h-4 w-4" />Allowed internal Team links only</CardTitle></CardHeader><CardContent className="grid gap-2 text-sm text-muted-foreground"><p>Review related internal surfaces only. These links do not trigger generation or external action.</p><div className="flex flex-wrap gap-2">{MOMO_AI_DRAFT_ALLOWED_TEAM_LINKS.map((href) => <Button asChild key={href} size="sm" variant="outline"><Link href={href}>{href}</Link></Button>)}<Button asChild size="sm" variant="outline"><Link href="/team/momo-ai-approval">Review AI approval queue</Link></Button></div></CardContent></Card><Card className="mt-4"><CardContent className="p-4"><Button asChild size="sm" variant="outline"><Link href="/team/momo-dry-run-go-no-go">Review internal dry run gate</Link></Button></CardContent></Card><TeamReviewModeRouteSummary title="Momo Controlled AI Draft Generation Foundation review-mode summary" /></PortalLayout>; }
