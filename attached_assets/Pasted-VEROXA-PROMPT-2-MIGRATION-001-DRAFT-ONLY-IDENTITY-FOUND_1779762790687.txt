VEROXA PROMPT 2 — MIGRATION 001 DRAFT ONLY: IDENTITY FOUNDATION

Objective:
Create a draft-only Migration 001 file for Veroxa’s identity foundation.

This is NOT a live migration.
This is NOT to be run yet.
This is a draft migration file for review only.

Do NOT run migrations.
Do NOT connect Supabase.
Do NOT activate real auth.
Do NOT change AUTH_MODE.
Do NOT change auth guards.
Do NOT change portal UI.
Do NOT change pricing.
Do NOT change navigation.
Do NOT build uploads.
Do NOT build AI APIs.
Do NOT build publishing.
Do NOT add payment processing.

Keep AUTH_MODE as:
"placeholder"

------------------------------------------------

SOURCE FILES TO READ FIRST

Read:

docs/SUPABASE_SCHEMA_DRAFT_V1.md
docs/SUPABASE_RLS_PLAN_V1.md
docs/sql_drafts/001_veroxa_schema_draft.sql
docs/sql_drafts/002_rls_policy_examples.sql

Only proceed after aligning with these planning documents.

------------------------------------------------

PART 1 — CREATE DRAFT MIGRATION FILE IN SAFE LOCATION

Create:

docs/sql_drafts/migrations_review/001_identity_foundation_draft.sql

Important:
Do NOT put this file inside supabase/migrations.
Do NOT run it.
Do NOT apply it.

Add this header:

DO NOT RUN — MIGRATION REVIEW DRAFT ONLY
This file is not active. It is not in the Supabase migrations folder.
Review and audit before converting into a real migration.

------------------------------------------------

PART 2 — TABLES TO INCLUDE

Draft only these tables:

1. user_profiles
2. team_members

Do NOT create clients yet.
Do NOT create media tables yet.
Do NOT create reports yet.
Do NOT create posts yet.
Do NOT create AI tables yet.

------------------------------------------------

PART 3 — user_profiles TABLE DRAFT

Draft:

user_profiles

Fields:
- id uuid primary key references auth.users(id) on delete cascade
- display_name text not null
- email text not null unique
- role text not null check role in ('client','team','operator','owner')
- client_id uuid nullable
- avatar_url text nullable
- is_active boolean not null default true
- created_at timestamptz not null default now()
- updated_at timestamptz not null default now()

Notes:
- client_id FK cannot be added until clients table exists in Migration 002
- document this clearly in a SQL comment
- role must stay limited to client/team/operator/owner
- system/service role is not a user_profile role

------------------------------------------------

PART 4 — team_members TABLE DRAFT

Draft:

team_members

Fields:
- id uuid primary key default gen_random_uuid()
- user_profile_id uuid not null unique references user_profiles(id) on delete cascade
- role_label text not null
- is_active boolean not null default true
- created_at timestamptz not null default now()
- updated_at timestamptz not null default now()

Important:
Do NOT use assigned_client_ids uuid[].

Team-to-client assignments will be handled in Migration 002 using team_client_assignments.

Add SQL comment:
Client assignments are intentionally excluded from Migration 001 and will be handled by team_client_assignments in Migration 002.

------------------------------------------------

PART 5 — UPDATED_AT TRIGGER DRAFT

If safe, include a generic updated_at trigger function draft:

set_updated_at()

Use it for:
- user_profiles
- team_members

Keep it simple.

Do not over-engineer.

------------------------------------------------

PART 6 — HELPER FUNCTION DRAFTS

Draft helper functions:

private.current_user_role()
private.current_user_client_id()
private.is_owner()
private.is_operator()
private.is_team_member()
private.is_system_actor()

Rules:
- schema should be explicit
- use security definer if needed
- set search_path explicitly
- stable where appropriate
- return null/false instead of throwing
- no dynamic SQL

Do not include is_assigned_to_client yet if it depends on clients/team_client_assignments from Migration 002.

If included, mark it deferred until Migration 002.

------------------------------------------------

PART 7 — RLS DRAFT

Enable RLS on:

user_profiles
team_members

Draft policies:

user_profiles:
- user can select own profile
- owner can select all profiles
- owner can update all profiles
- operator can select all profiles but not update role
- client/team cannot update role

team_members:
- team user can select own team_member row
- operator can select all team members
- owner can select/update all team members

Do not allow public access.

Do not allow anonymous access.

------------------------------------------------

PART 8 — INDEX DRAFTS

Add indexes:

user_profiles(role)
user_profiles(email)
team_members(user_profile_id)
team_members(is_active)

Do not over-index.

------------------------------------------------

PART 9 — TEST PLAN DOCUMENT

Create or update:

docs/MIGRATION_001_TEST_PLAN.md

Include tests for:

- anon cannot select user_profiles
- client sees only own profile
- team sees only own profile/team_member row
- operator can view all profiles
- owner can view/update profiles
- owner can change role
- non-owner cannot change role
- current_user_role() returns correct role
- inactive users handling is documented
- service role/system behavior documented

No tests need to be automated yet, but list them clearly.

------------------------------------------------

PART 10 — FINAL REPORT REQUIRED

At completion, report:

1. Files created
2. Tables drafted
3. Helper functions drafted
4. RLS policies drafted
5. Indexes drafted
6. What was intentionally deferred to Migration 002
7. Confirmation file is NOT in supabase/migrations
8. Confirmation migration was NOT run
9. Confirmation AUTH_MODE unchanged
10. Confirmation pricing unchanged
11. Risks before converting to real migration
12. Recommended next prompt

PASS STANDARD:
- Migration 001 draft exists only in docs/sql_drafts/migrations_review/
- No live migrations run
- No app behavior changes
- No auth mode changes
- Identity foundation is ready for review
- team_client_assignments is not incorrectly placed in Migration 001