You are working on the Veroxa Growth OS repo.

This is a documentation/status stabilization pass only.

Do not add features. Do not change runtime behavior. Do not change routing. Do not change navigation. Do not change pricing. Do not connect any backend.

The ZIP/current workspace is authoritative.

Context:
The repo has many historical status sections in BUILD_STATUS.md. Some are valid as history, but future agents may misread older sections as current instructions. The goal of this pass is to make the current Veroxa state unmistakable and prevent stale guidance from causing unsafe next steps.

Hard invariants — do not violate any of these:

1. AUTH_MODE stays the literal string "placeholder".
2. No file under supabase/migrations/. All SQL stays under artifacts/veroxa/docs/sql_drafts/.
3. Portal stays disconnected from any real database.
4. No real AI provider wired. ai_agents.is_enabled=true stays inert.
5. ai_agents.config_json never contains secrets / API keys / bearer tokens / signed URLs.
6. Demo gate veroxa-preview stays in place.
7. Locked pricing unchanged:
   GPS 49700,
   COP 12mo 99700,
   COP 6mo 109700,
   COP 3mo 119700,
   COP no-contract 149700.
8. Roles exactly Client / Team / Operator / Owner.
9. Real client / restaurant / customer data: zero. Fixtures only.
10. No nav routing or 4-shell changes.

Human dev-test gate:
Humans have NOT yet run M001–M006 on the dev Supabase project. Until those tests are run and trackers are green, do not:
- promote SQL to supabase/migrations/,
- flip AUTH_MODE,
- connect the portal to Supabase,
- add post-execution follow-ups based on real dev-test results,
- replace fixtures with database-backed runtime data.

Tasks:

T01 — Add a current-state summary at the very top of BUILD_STATUS.md

Touch only:
- artifacts/veroxa/docs/BUILD_STATUS.md

Goal:
Make the first screen of BUILD_STATUS.md clearly state the current truth.

Acceptance criteria:
- Top section says AUTH_MODE is placeholder.
- Top section says real auth code may exist but is inactive.
- Top section says SQL is draft/dev-test only and not promoted.
- Top section says M001–M006 still require human dev Supabase execution.
- Top section says portal remains fixture/demo-first.
- Top section says no real AI, publishing, uploads, storage, Google integration, or writes are active.
- Top section links to PORTAL_QUERY_SAFETY_PLAN.md, CLIENT_HEALTH_ENGINE_CONTRACT.md, CLIENT_HEALTH_SURFACE_MAP.md, and docs/sql_drafts/dev_test/README.md.

T02 — Add a “Do not follow old changelog sections as current instructions” warning

Touch only:
- artifacts/veroxa/docs/BUILD_STATUS.md

Goal:
Prevent older dated sections from being mistaken as current next steps.

Acceptance criteria:
- Warning appears near the top.
- It explains older sections are historical changelog entries.
- It says the current-state summary overrides older “next phase” language.
- It specifically warns not to follow old real-auth flip notes until the human dev-test gate is cleared.

T03 — Add a current next-step ladder

Touch only:
- artifacts/veroxa/docs/BUILD_STATUS.md

Goal:
Define the correct sequence from here.

Acceptance criteria:
BUILD_STATUS.md contains a short current next-step ladder in this order:
1. Keep demo/placeholder mode.
2. Human runs M001–M006 dev-test packages manually.
3. Human records pass/fail in trackers.
4. Only after green trackers, consider post-execution SQL/auth planning.
5. Separately, safe demo-only work may continue on fixture coherence and health-engine consolidation.

T04 — Update docs index with current status links

Touch only:
- artifacts/veroxa/docs/README.md

Goal:
Make docs/README.md point to the current safety/status documents.

Acceptance criteria:
- docs/README.md links to CLIENT_HEALTH_ENGINE_CONTRACT.md.
- docs/README.md links to CLIENT_HEALTH_SURFACE_MAP.md.
- docs/README.md links to PORTAL_QUERY_SAFETY_PLAN.md.
- docs/README.md links to PORTAL_QUERY_SAFETY_CHECKLIST.md.
- docs/README.md links to docs/sql_drafts/dev_test/README.md.
- It states these are current safety/status references before auth/backend work.

T05 — Add a short “allowed next prompt themes” section

Touch only:
- artifacts/veroxa/docs/BUILD_STATUS.md

Goal:
Clarify what Replit may safely work on before the dev-test gate clears.

Acceptance criteria:
Allowed themes listed:
- docs/status cleanup,
- fixture coherence,
- health-engine consolidation,
- demo-only UI polish that does not alter routing/nav/shell model,
- route registry audits.

Forbidden themes listed:
- AUTH_MODE flip,
- Supabase connection,
- SQL promotion,
- real writes/uploads/storage,
- real AI provider,
- publishing/Google integrations,
- pricing changes,
- role model changes,
- nav/routing changes.

After completing tasks:
1. Run pnpm --filter @workspace/veroxa run typecheck if pnpm is available.
2. Run pnpm --filter @workspace/scripts run check-demo-routes if pnpm is available.
3. If commands cannot run, say exactly why.
4. Perform an architect/code-review pass manually.

Final report must explicitly confirm:

- Confirm invariant 1: AUTH_MODE remains literal "placeholder".
- Confirm invariant 2: no file added to supabase/migrations/.
- Confirm invariant 3: portal remains disconnected from any real database.
- Confirm invariant 4: no real AI provider wired; ai_agents.is_enabled=true remains inert.
- Confirm invariant 5: ai_agents.config_json contains no secrets, API keys, bearer tokens, or signed URLs.
- Confirm invariant 6: demo gate veroxa-preview remains in place.
- Confirm invariant 7: locked pricing values unchanged.
- Confirm invariant 8: roles remain Client / Team / Operator / Owner.
- Confirm invariant 9: fixtures only; no real client/customer data added.
- Confirm invariant 10: no navigation routing or four-shell changes.

End report with:
- Files changed
- Commands run
- Typecheck result
- Route-check result
- Architect review result
- Any open questions