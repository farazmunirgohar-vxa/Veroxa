VEROXA PROMPT — DEMO INTERNAL ACCESS GATE FOR TEAM / OPERATOR / OWNER

Objective:
Fix the mismatch where Team, Operator, and Owner demo portals say “Login Required” but open freely in placeholder mode.

This is NOT real authentication.
This is NOT Supabase auth.
This is NOT a backend change.
This is a demo-only access gate for internal preview portals.

Do NOT run migrations.
Do NOT move files into supabase/migrations.
Do NOT connect Supabase.
Do NOT activate real auth.
Do NOT change AUTH_MODE.
Do NOT change pricing.
Do NOT change navigation.
Do NOT change backend docs unless adding a short note.
Do NOT build uploads.
Do NOT add AI APIs.
Do NOT build publishing.
Do NOT add payments.

Keep AUTH_MODE as:

"placeholder"

Keep pricing unchanged:

Google Presence Starter:
$497/mo

Complete Online Presence:
12-month: $997/mo
6-month: $1,097/mo
3-month: $1,197/mo
No-contract: $1,497/mo

------------------------------------------------

CURRENT ISSUE

Team, Operator, and Owner role cards show “Login Required,” but the internal demo portals open without credentials because InternalDemoGuard bypasses auth when AUTH_MODE is "placeholder".

This is confusing, especially on mobile.

We need a demo-only gate so internal portals feel protected without activating real auth.

------------------------------------------------

PART 1 — KEEP CLIENT PORTAL PUBLIC

Client Portal should remain public preview.

Do not gate:

/demo/client/*
/demo/client/dashboard
/demo/client/media
/demo/client/calendar
/demo/client/reports
/demo/client/requests
/demo/client/account

Client role card can continue to show:

Public Preview

------------------------------------------------

PART 2 — ADD DEMO INTERNAL ACCESS GATE

Create a small demo-only access gate for:

/demo/team/*
/demo/operator/*
/demo/owner/*

This gate should work only while AUTH_MODE is "placeholder".

It should NOT use Supabase.
It should NOT call APIs.
It should NOT store passwords.
It should NOT claim production security.

Recommended behavior:

When a user opens Team, Operator, or Owner demo portal without demo access:

Show a clean Veroxa access screen:

Title:
Internal Demo Access

Subtitle:
This is a protected Veroxa preview area. Real authentication is not active yet.

Input:
Demo access code

Button:
Enter Demo Portal

Small note:
Demo-only gate. Real role-based login will activate after Supabase auth is reviewed.

Allowed demo code:
veroxa-preview

On success:
Store temporary demo access in localStorage or sessionStorage:

veroxa_demo_internal_access = true

Then show the requested portal.

Add a small “Clear demo access” link or button somewhere simple if practical.

------------------------------------------------

PART 3 — UPDATE InternalDemoGuard BEHAVIOR

File:

src/components/auth/InternalDemoGuard.tsx

Current behavior:
If AUTH_MODE === "placeholder", it renders children unconditionally.

Change behavior:

If AUTH_MODE === "placeholder":

- For internal demo roles, check demo internal access state.
- If access exists, render children.
- If access does not exist, show the demo internal access gate.
- Do not call useAuth in placeholder mode.
- Do not require Supabase.
- Do not weaken real auth path.

If AUTH_MODE === "real":

Keep the existing real-auth path:

- loading
- unauthenticated
- wrong role
- matching role

Do not break future real auth.

------------------------------------------------

PART 4 — ROLE-AWARE MESSAGING

The access gate should know which portal is being opened:

Team Portal
Operator Portal
Owner Portal

Display copy like:

Team Portal Preview
Operator Portal Preview
Owner Portal Preview

Do not expose internal role logic to Client Portal.

------------------------------------------------

PART 5 — UPDATE LOGIN PAGE COPY

File:

src/pages/login.tsx

Current badge says:

Login Required

For Team, Operator, and Owner, change badge to something more accurate:

Internal Preview

or:

Demo Access Required

Recommended:

Demo Access Required

This avoids pretending real auth is live.

Client badge remains:

Public Preview

Do not change the route links.

Current role links should remain:

Team:
/demo/team/dashboard

Operator:
/demo/operator/operator-os

Owner:
/demo/owner/executive-dashboard

------------------------------------------------

PART 6 — MOBILE UX

Make the demo access gate mobile-friendly.

Check:

- input width
- button width
- vertical spacing
- text size
- no horizontal overflow
- works on iPhone-sized viewport

The screen should feel premium and simple.

------------------------------------------------

PART 7 — DOCUMENT THE BEHAVIOR

Add a short note to an appropriate doc, such as:

docs/AUTH_MODE_SWITCH_PLAN.md

or

docs/AUTH_QA_CHECKLIST.md

Explain:

- AUTH_MODE remains placeholder
- Team/Operator/Owner demo portals use a demo-only access gate
- This is not real authentication
- Real Supabase auth will replace it later
- Client Portal remains public preview

Do not over-document.

------------------------------------------------

PART 8 — VALIDATION

Verify these routes:

/login
/demo/client/dashboard
/demo/team/dashboard
/demo/operator/operator-os
/demo/owner/executive-dashboard

Expected behavior:

/demo/client/dashboard:
opens publicly

/demo/team/dashboard:
requires demo access code first

/demo/operator/operator-os:
requires demo access code first

/demo/owner/executive-dashboard:
requires demo access code first

After entering correct code:
internal demo portals open

Wrong code:
show a friendly error

Refresh:
access persists if localStorage is used

Clear demo access:
internal gate returns

------------------------------------------------

PART 9 — DO NOT TOUCH BACKEND MIGRATION WORK

Do not modify:

docs/sql_drafts/migrations_review/001_identity_foundation_draft.sql
docs/sql_drafts/migrations_review/002_client_foundation_draft.sql
docs/sql_drafts/migrations_review/003_media_foundation_draft.sql
docs/MIGRATION_004_POSTING_FOUNDATION_PLAN.md

unless adding a tiny note is absolutely necessary.

This task is about demo access UX only.

------------------------------------------------

FINAL REPORT REQUIRED

At completion, report:

1. Files modified
2. InternalDemoGuard behavior changed
3. Demo access code used
4. Login page badge changes
5. Client portal remains public
6. Team/Operator/Owner now require demo access
7. Mobile fixes made
8. Confirmation AUTH_MODE unchanged
9. Confirmation Supabase auth not activated
10. Confirmation no migrations were run
11. Confirmation pricing unchanged
12. Build/typecheck result
13. Remaining risks

PASS STANDARD:
- Client demo remains public
- Team/Operator/Owner demo portals require demo access code in placeholder mode
- Real auth path remains untouched
- AUTH_MODE remains placeholder
- No backend/migration changes
- Mobile gate looks clean