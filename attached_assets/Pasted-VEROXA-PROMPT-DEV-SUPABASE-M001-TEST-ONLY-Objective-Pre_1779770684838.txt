VEROXA PROMPT — DEV SUPABASE M001 TEST ONLY

Objective:
Prepare and execute a dev-only Supabase test of Migration 001 identity foundation.

This is DEV TESTING ONLY.

Do NOT apply M002.
Do NOT apply M003.
Do NOT apply M004.
Do NOT apply M005.
Do NOT apply M006.
Do NOT apply portal-connect views.
Do NOT activate real auth in the app.
Do NOT change AUTH_MODE.
Do NOT connect the Veroxa portal to Supabase.
Do NOT issue real client credentials.
Do NOT use production data.
Do NOT add new schema beyond M001.
Do NOT change pricing.
Do NOT change navigation.
Do NOT change portal UI.

Keep:

AUTH_MODE = "placeholder"

------------------------------------------------
SOURCE FILES
------------------------------------------------

Use only:

docs/sql_drafts/migrations_review/001_identity_foundation_draft.sql
docs/MIGRATION_001_TEST_PLAN.md

Reference only if needed:

docs/SUPABASE_SCHEMA_DRAFT_V1.md
docs/SUPABASE_RLS_PLAN_V1.md

------------------------------------------------
PART 1 — DEV SUPABASE SETUP CHECK
------------------------------------------------

Use a DEV Supabase project only.

Confirm:

- This is not production.
- No real client data exists.
- No real restaurant data exists.
- No real client credentials are being issued.
- AUTH_MODE remains "placeholder" in the app.
- The portal is not connected to this dev database yet.

------------------------------------------------
PART 2 — APPLY ONLY M001
------------------------------------------------

Apply only:

docs/sql_drafts/migrations_review/001_identity_foundation_draft.sql

Do not apply any other SQL draft.

Do not apply:

002_client_foundation_draft.sql
003_media_foundation_draft.sql
004_posting_foundation_draft.sql
005_reporting_foundation_draft.sql
006_content_ai_layer_draft.sql
002_003_004_portal_connect_views_draft.sql
003_notifications_status_guard_draft.sql
004_post_slot_reset_guard_draft.sql

------------------------------------------------
PART 3 — CREATE DEV TEST USERS
------------------------------------------------

Create one dev Supabase Auth user for each role:

- client
- team
- operator
- owner

Then create matching rows in:

public.user_profiles

Use fake/dev emails only, such as:

client@veroxa.test
team@veroxa.test
operator@veroxa.test
owner@veroxa.test

No real client emails.

Create team_members row only for the team user if required by the M001 test plan.

------------------------------------------------
PART 4 — RUN M001 TEST PLAN
------------------------------------------------

Run every test in:

docs/MIGRATION_001_TEST_PLAN.md

Record pass/fail for each test.

Minimum required coverage:

- anon cannot select user_profiles
- anon cannot select team_members
- client can select own profile only
- client cannot select another user profile
- team can select own profile
- team can select own team_member row
- operator can view all profiles
- operator cannot change role
- owner can view all profiles
- owner can update profiles
- owner can change roles
- non-owner cannot change role
- non-owner cannot change client_id
- non-owner cannot change email
- non-owner cannot change is_active
- safe self-update works only for allowed fields
- helper functions return expected values:
  - current_user_role()
  - current_user_client_id()
  - is_owner()
  - is_operator()
  - is_team_member()
  - is_system_actor()

------------------------------------------------
PART 5 — STOP ON FAILURE
------------------------------------------------

If any test fails:

STOP.

Do not patch the database silently.
Do not apply M002.
Do not change AUTH_MODE.
Do not connect the app.

Return the failure details.

------------------------------------------------
PART 6 — FINAL REPORT REQUIRED
------------------------------------------------

Return:

1. Dev Supabase project confirmed
2. M001 applied or failed
3. Any SQL errors during M001
4. Test users created
5. Test plan results, pass/fail by section
6. Helper function results
7. RLS results
8. Any failed tests
9. Any unexpected behavior
10. Confirmation M002–M006 were NOT applied
11. Confirmation AUTH_MODE stayed placeholder
12. Confirmation portal was not connected
13. Recommendation:
   - M001 green, ready to plan M002 dev test
   - M001 needs correction
   - Stop, major issue found

PASS STANDARD:
- Only M001 applied
- All M001 tests pass
- No production data used
- No portal connection activated
- AUTH_MODE remains placeholder
- No M002–M006 schema applied