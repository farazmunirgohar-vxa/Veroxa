VEROXA PROMPT — STABILIZATION PASS: AUTH ROLLBACK + ROUTE CONSISTENCY + LOCKED PRICING CLEANUP

Objective:
Stabilize the Veroxa demo app before adding any new features.

Do NOT add new features.
Do NOT add new pages.
Do NOT add new dashboards.
Do NOT add new Supabase functionality.
Do NOT add new auth functionality.
Do NOT add AI APIs.
Do NOT add uploads.
Do NOT add publishing.
Do NOT delete major routes yet.

This is a stability, consistency, and cleanup pass only.

------------------------------------------------

1. FIX AUTH MODE REGRESSION

File:
src/lib/auth/authMode.ts

The demo app should NOT require real Supabase auth yet.

Make sure AUTH_MODE is locked to:

export const AUTH_MODE: AuthMode = "placeholder";

Do not remove real auth files.
Do not remove Supabase files.
Do not delete future auth scaffolding.

Goal:
The Veroxa demo portals should remain previewable without real Supabase credentials.

------------------------------------------------

2. DEMO ACCESS SHOULD WORK WITHOUT REAL SUPABASE

Review:

src/components/auth/InternalDemoGuard.tsx
src/lib/auth/useAuth.ts
src/lib/auth/usePlaceholderAuth.ts
src/lib/auth/useRealAuth.ts

In placeholder/demo mode:

- Client demo should open
- Team demo should open
- Operator demo should open
- Owner demo should open

Do not block demo portals because real Supabase auth is not configured.

Real authenticated routes can remain future placeholders.

------------------------------------------------

3. UPDATE LOGIN CARD ROUTES

File:
src/pages/login.tsx

Update role card links so they point to the current consolidated demo home routes:

Client:
  /demo/client/dashboard

Team:
  /demo/team/dashboard

Operator:
  /demo/operator/operator-os

Owner:
  /demo/owner/executive-dashboard

Remove old login card links such as:

/demo/team/tasks
/demo/operator/overview
/demo/owner/dashboard

------------------------------------------------

4. UPDATE DEMO ROLE HOME PATHS

File:
src/lib/auth/authContract.ts

Update DEMO_ROLE_HOME_PATH to match the current consolidated demo home routes:

client: /demo/client/dashboard
team: /demo/team/dashboard
operator: /demo/operator/operator-os
owner: /demo/owner/executive-dashboard

Do not change future real route paths unless necessary.

------------------------------------------------

5. VERIFY LOCKED VEROXA BASE PRICING EVERYWHERE

Search the codebase for all pricing references.

Locked Veroxa base pricing is:

12-month plan: $997/month
6-month plan: $1,097/month
3-month plan: $1,197/month
No-contract plan: $1,497/month

Make sure these are the only base pricing numbers used for Complete Online Presence.

Search for and correct outdated pricing references including:

$1000
$1100
$1200
$1500 as base pricing
$1,000
$1,100
$1,200
997 if used incorrectly
1097 if formatted inconsistently
1197 if formatted inconsistently
1497 if formatted inconsistently
7-month plan
any invented plan length

Important:
Do NOT replace the locked pricing with older pricing.
Do NOT invent new plans.
Do NOT use 7-month plans.

Files likely affected:

src/pages/pricing.tsx
src/pages/owner-os.tsx
src/data/demoData.ts
src/domain/clientHealth/engine.ts

Pricing should be displayed cleanly as:

$997/mo
$1,097/mo
$1,197/mo
$1,497/mo

------------------------------------------------

6. DO NOT CHANGE ADS OR BUNDLE PRICING UNLESS ALREADY CLEAR

This pass is mainly for base pricing consistency.

Do not invent ad pricing.
Do not invent bundle pricing.
Do not restructure the pricing page beyond correcting obvious inconsistency.

If ads or bundles already exist in the app, leave them unless they clearly conflict with existing Veroxa pricing records.

------------------------------------------------

7. FIX NAVIGATION TO INTERNAL PAGES

Review:

src/lib/operatorPortalNav.ts
src/lib/ownerPortalNav.ts
src/lib/clientPortalNav.ts
src/lib/teamPortalNav.ts

Any page visible in portal navigation should feel like part of that portal.

If a visible nav item points to /demo/internal/..., either:

Option A:
Wrap that page in the correct PortalLayout based on role context

OR

Option B:
Point the nav item to an existing role-specific page instead.

Do not expose raw development-only pages as normal portal items.

Internal/dev pages should not feel like client-facing or operator-facing product pages.

------------------------------------------------

8. WRAP PRIMARY OS PAGES IN PORTAL LAYOUT

Review these pages:

src/pages/operator-os.tsx
src/pages/owner-os.tsx
src/pages/operator-media-library.tsx
src/pages/client-health-command.tsx
src/pages/internal-system-status.tsx

If any of these are reachable from visible portal navigation, they should use PortalLayout with the correct portal nav items.

Do not change their business logic.
Do not add features.
Only make the UX consistent.

------------------------------------------------

9. SYNC DEMO ROUTE REGISTRY

File:
src/lib/demoRoutes.ts

Update demoRoutes.ts so it honestly reflects the current route structure.

At minimum, include the current visible routes for:

Client
Team
Operator
Owner

Mark old, hidden, internal, or legacy routes clearly as:

hidden_from_nav
legacy_demo
internal_demo
future_protected

Do not pretend demoRoutes.ts is complete if it is not.

Goal:
Make route documentation useful again for future auth and permission work.

------------------------------------------------

10. REDUCE OBVIOUS DUPLICATE HOME CONFUSION WITHOUT DELETING ROUTES

Do not delete route files.

But make sure navigation clearly points to only one primary home per role:

Client:
Dashboard

Team:
Dashboard

Operator:
Operator OS / Command Center

Owner:
Executive Dashboard

If duplicate home pages still exist, keep them hidden from navigation for now.

------------------------------------------------

11. FINAL REPORT REQUIRED

At completion, report:

1. AUTH_MODE final value
2. Demo access behavior
3. Login routes updated
4. Demo home paths updated
5. Pricing files corrected
6. Any outdated pricing found and fixed
7. Internal nav links corrected
8. Pages wrapped in PortalLayout
9. demoRoutes.ts sync status
10. Files modified
11. Build result
12. Any remaining risks

No new features.
No new pages.
No real backend work.
This is a stabilization pass only.