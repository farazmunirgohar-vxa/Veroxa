VEROXA MAIN AGENT PROMPT — FULL POWER STABILIZATION + CLARITY + HYGIENE PASS

Objective:
Use the findings from the 3 parallel review agents and perform one controlled implementation pass to stabilize Veroxa.

This is NOT a feature expansion.
This is NOT a redesign.
This is NOT real backend implementation.

This is a major cleanup/stabilization pass to make Veroxa clearer, safer, and more maintainable before we add real database/auth/uploads/AI later.

------------------------------------------------

IMPORTANT RULES

Do NOT add new features.
Do NOT add new portals.
Do NOT add new dashboards.
Do NOT connect Supabase further.
Do NOT add real authentication.
Do NOT add OpenAI/Anthropic APIs.
Do NOT build real uploads.
Do NOT build real publishing.
Do NOT run migrations.
Do NOT delete major route files unless explicitly safe.
Do NOT invent pricing.
Do NOT expose internal pages to clients.

Prefer:
- Stabilize existing code
- Hide duplicate pages from navigation
- Wrap visible pages consistently
- Fix route/documentation mismatches
- Reduce visible noise
- Improve maintainability
- Preserve working routes

------------------------------------------------

SOURCE OF TRUTH

Veroxa roles:

Client:
Uploads media, views calendar, views reports, sends requests, manages account.

Team:
Executes media review, drafts, scheduling, weekly report validation, and alerts.

Operator:
Oversees client health, quality, alerts, failed posts, monthly report approvals, and team oversight.

Owner:
Sees revenue, growth, retention, client risk, critical alerts, and AI/system health.

Locked Veroxa base pricing:

12-month plan: $997/mo
6-month plan: $1,097/mo
3-month plan: $1,197/mo
No-contract plan: $1,497/mo

Do not use older pricing.

------------------------------------------------

PART 1 — READ PARALLEL AGENT REPORTS FIRST

If reports from Parallel Agent 1, 2, and 3 are available in the Replit chat, read them first.

Agent 1 focus:
Technical stabilization, auth mode, routes, pricing, portal layout, demo route registry.

Agent 2 focus:
Product clarity, role separation, dashboard noise, portal UX.

Agent 3 focus:
Codebase hygiene, demo data bloat, route sprawl, backend readiness, component duplication.

Before changing files, produce a short internal action plan:

1. Critical fixes
2. Safe UX cleanup
3. Code hygiene fixes
4. Things to avoid in this pass

Then implement only safe changes.

------------------------------------------------

PART 2 — AUTH MODE STABILIZATION

Inspect:

src/lib/auth/authMode.ts
src/components/auth/InternalDemoGuard.tsx
src/lib/auth/useAuth.ts
src/lib/auth/usePlaceholderAuth.ts
src/lib/auth/useRealAuth.ts

Goal:
The demo app must remain previewable without real Supabase credentials.

Make sure:

export const AUTH_MODE: AuthMode = "placeholder";

Do not remove real auth scaffolding.
Do not remove Supabase files.
Do not delete future auth files.

But in placeholder/demo mode:

- /demo/client/dashboard opens
- /demo/team/dashboard opens
- /demo/operator/operator-os opens
- /demo/owner/executive-dashboard opens

Demo portals should not be blocked because real Supabase is not configured.

------------------------------------------------

PART 3 — LOGIN ROUTE CONSISTENCY

Inspect:

src/pages/login.tsx
src/lib/auth/authContract.ts
src/App.tsx
src/lib/demoRoutes.ts

Update role card links to current consolidated demo homes:

Client:
  /demo/client/dashboard

Team:
  /demo/team/dashboard

Operator:
  /demo/operator/operator-os

Owner:
  /demo/owner/executive-dashboard

Remove stale login card destinations such as:

/demo/team/tasks
/demo/operator/overview
/demo/owner/dashboard

Update DEMO_ROLE_HOME_PATH in authContract.ts to match:

client: /demo/client/dashboard
team: /demo/team/dashboard
operator: /demo/operator/operator-os
owner: /demo/owner/executive-dashboard

Do not break real future route placeholders.

------------------------------------------------

PART 4 — LOCKED PRICING CONSISTENCY

Search the entire codebase for pricing references.

Locked Veroxa base pricing is:

12-month plan: $997/mo
6-month plan: $1,097/mo
3-month plan: $1,197/mo
No-contract plan: $1,497/mo

Find and correct outdated or inconsistent references, including:

$1000
$1100
$1200
$1500 as base pricing
$1,000
$1,100
$1,200
7-month plans
invented plan lengths
incorrect contract terms
wrong ordering of pricing tiers

Pricing should display cleanly as:

$997/mo
$1,097/mo
$1,197/mo
$1,497/mo

Likely files to inspect:

src/pages/pricing.tsx
src/pages/owner-os.tsx
src/data/demoData.ts
src/domain/clientHealth/engine.ts
any pricing-related components
any pricing-related domain files

Do not invent ad pricing.
Do not invent bundle pricing.
If ads/bundles already exist and are not clearly wrong, leave them unchanged unless they conflict with locked records.

------------------------------------------------

PART 5 — PORTAL NAVIGATION CLEANUP

Inspect:

src/lib/clientPortalNav.ts
src/lib/teamPortalNav.ts
src/lib/operatorPortalNav.ts
src/lib/ownerPortalNav.ts

Target visible nav limits:

Client: 6 max
Team: 7 max
Operator: 7 max
Owner: 7 max

Preferred visible navigation:

CLIENT:
- Dashboard
- Upload Media
- Calendar
- Reports
- Requests
- Account

TEAM:
- Dashboard
- Work Queue
- Media Review
- Drafts
- Scheduling
- Reports
- Alerts

OPERATOR:
- Operator OS / Command Center
- Client Health
- Alerts
- Report Approvals
- Media Library
- Team Oversight
- System Status

OWNER:
- Executive Dashboard
- Revenue
- Client Health
- Critical Alerts
- AI/System Health
- Growth
- Settings

Rules:

- No duplicate concepts in visible nav.
- No raw /demo/internal/... pages in normal portal nav unless they are wrapped and styled as portal pages.
- Client should not see AI/internal/system language.
- Team should not see revenue/growth strategy.
- Operator should not be turned into daily execution.
- Owner should not be pulled into daily content operations.

Hide from navigation, but do not delete routes yet.

------------------------------------------------

PART 6 — PRIMARY HOMEPAGE CONSOLIDATION

Make sure each role has only one obvious primary home route.

Client:
Primary home = client dashboard

Team:
Primary home = team dashboard

Operator:
Primary home = operator-os or command center page
Use /demo/operator/operator-os as the route

Owner:
Primary home = executive dashboard
Use /demo/owner/executive-dashboard as the route

If duplicate pages exist, keep them hidden from navigation.

Examples of duplicate/overlapping pages to review:

Owner:
- owner-dashboard
- owner-command-center
- owner-executive-dashboard
- owner-os

Operator:
- operator-os
- operator-overview
- operator-command-board

Team:
- team-dashboard
- team-task-engine
- team-tasks

AI:
- aiagentsview
- agent-preview
- owner-ai-agents
- owner-ai-agents-v2

Do not delete these files in this pass unless obviously unused and safe.
Prefer hiding from nav and marking as legacy/internal in demoRoutes.ts.

------------------------------------------------

PART 7 — PORTAL LAYOUT CONSISTENCY

Every page visible from a portal sidebar should feel like part of the portal.

Inspect visible nav routes and check whether the destination pages use the correct PortalLayout.

Review especially:

src/pages/operator-os.tsx
src/pages/owner-os.tsx
src/pages/operator-media-library.tsx
src/pages/client-health-command.tsx
src/pages/internal-system-status.tsx
all visible client pages
all visible team pages
all visible operator pages
all visible owner pages

If visible from a portal nav:

- Use PortalLayout
- Use correct role context
- Use correct navigation
- Keep visual consistency

Do not change business logic.
Do not add features.
Just make the visible UX consistent.

------------------------------------------------

PART 8 — CLIENT PORTAL SIMPLIFICATION

Client portal should feel simple and premium.

Client should understand:

“Upload media, see upcoming posts, view reports, send requests.”

Inspect client pages for clutter:

- client-dashboard
- client-media
- client-calendar
- client-reports
- client-requests
- client-account

Simplify if obviously too noisy.

Remove or hide client-facing content that feels:

- too internal
- too technical
- too AI-heavy
- too team/operator-focused
- too full of metrics
- too full of cards

Client should NOT see:

- internal draft history
- internal rejection reasoning
- team workflow details
- operator risk logic
- owner/business metrics
- raw AI agent language

Keep client dashboard focused on:

- upload status
- upcoming posts
- simple performance snapshot
- reports
- recent activity
- requests/account access

Do not remove useful client visibility.

------------------------------------------------

PART 9 — TEAM PORTAL SIMPLIFICATION

Team portal should feel execution-focused.

Team should quickly know:

- what media needs review
- what drafts need work
- what posts need scheduling
- what reports need validation
- what alerts need action

Review duplicate task/work pages:

- team-dashboard
- team-task-engine
- team-tasks
- work queue
- media review
- drafts
- scheduling
- reports
- alerts

If task-engine and tasks overlap, choose one visible destination.

Do not show team:

- owner revenue strategy
- high-level growth dashboards
- excessive analytics
- operator-only approval views

Keep Team focused on execution.

------------------------------------------------

PART 10 — OPERATOR PORTAL SIMPLIFICATION

Operator portal should feel like oversight and quality control.

Operator should see:

- client health
- alerts
- low-content clients
- failed/reschedule posts
- monthly report approvals
- team oversight
- system status

Operator should NOT approve every post.
Operator should NOT become daily execution.

Review and simplify/hide overlap:

- operator-os
- operator-overview
- operator-command-board
- operations center
- workflow engine
- action center
- daily digest
- priority board
- internal pages

Visible Operator pages should feel like:

- risk
- quality
- approvals
- health
- escalation

not daily production work.

------------------------------------------------

PART 11 — OWNER PORTAL SIMPLIFICATION

Owner portal should protect the owner from operations.

Owner should see:

- revenue
- active clients
- client health
- retention risk
- growth
- critical alerts
- AI/system health

Owner should NOT see:

- individual drafts
- daily task queues
- daily content pipeline
- weekly report management
- deep media operations

Review and simplify/hide overlap:

- owner-dashboard
- owner-command-center
- owner-executive-dashboard
- owner-os
- owner analytics pages
- AI pages
- automation pages
- activity/KPI pages

Keep Owner focused on:

money, growth, retention, risk, system health.

------------------------------------------------

PART 12 — DEMO ROUTE REGISTRY SYNC

Inspect:

src/App.tsx
src/lib/demoRoutes.ts

Goal:
demoRoutes.ts should become honest and useful again.

Update demoRoutes.ts so it includes at minimum:

- All visible Client routes
- All visible Team routes
- All visible Operator routes
- All visible Owner routes
- Key internal/demo routes

Mark routes clearly as:

visible_nav
hidden_from_nav
legacy_demo
internal_demo
future_protected

Do not pretend demoRoutes.ts is complete if it is not.
If App.tsx has many routes, document the visible/core routes first and mark the rest clearly.

This registry should help future auth and permission work.

------------------------------------------------

PART 13 — CODEBASE HYGIENE REVIEW + SAFE CLEANUP

Inspect:

src/pages
src/components
src/components/common
src/components/portal
src/domain
src/lib
src/data

Perform only safe cleanup:

- Remove obviously unused imports
- Fix obviously duplicated small helpers if safe
- Prefer existing shared components
- Do not rewrite large pages unnecessarily
- Do not split demoData.ts in this pass unless extremely safe

Create a future cleanup note/report for:

- large files
- duplicate components
- duplicate pages
- demoData split candidates
- future deletion candidates

Do not perform risky large refactors yet.

------------------------------------------------

PART 14 — DEMO DATA HYGIENE PLAN ONLY

Inspect:

src/data/demoData.ts
other demo data files

Do NOT perform a large data split unless it is very safe and low-risk.

But prepare a clear future split recommendation:

Suggested future structure:

src/data/demo/
  demoClients.ts
  demoMedia.ts
  demoPosts.ts
  demoReports.ts
  demoAgents.ts
  demoNotifications.ts
  demoFinancials.ts
  demoActivityLogs.ts
  index.ts

Check whether demo data aligns with Veroxa core objects:

- clients
- client_platforms
- onboarding_items
- media_assets
- content_concepts
- draft_sets
- draft_variants
- posts
- post_slots
- notifications
- weekly_reports
- monthly_reports
- activity_logs

Report what is missing or inconsistent.

Do not break imports.

------------------------------------------------

PART 15 — BACKEND READINESS CHECK ONLY

Do not build backend.

Do not create migrations.

But check whether frontend/domain/demo objects are aligned with future Veroxa backend objects:

clients
client_platforms
onboarding_items
media_assets
content_concepts
draft_sets
draft_variants
posts
post_slots
notifications
weekly_reports
monthly_reports
activity_logs

Report:

- well-represented objects
- missing objects
- inconsistent naming
- objects mixed together incorrectly
- risky assumptions before Supabase/auth

Do not implement database yet.

------------------------------------------------

PART 16 — BUILD + VALIDATION

After changes, run:

npm run build

If build fails:

- Fix only errors caused by this pass
- Do not introduce new features to solve build
- Keep fixes minimal

Also check:

- Login page role cards
- Client demo dashboard route
- Team demo dashboard route
- Operator demo home route
- Owner demo home route
- Portal sidebar navigation
- Pricing page
- No visible portal nav points to raw internal/dev pages unless wrapped properly

------------------------------------------------

PART 17 — FINAL REPORT REQUIRED

At completion, provide a detailed report:

1. Summary of what was fixed
2. AUTH_MODE final value
3. Demo access result
4. Login routes updated
5. DEMO_ROLE_HOME_PATH updated
6. Pricing references corrected
7. Visible nav counts before/after if available:
   - Client
   - Team
   - Operator
   - Owner
8. Pages hidden from navigation
9. Duplicate pages/concepts identified
10. PortalLayout pages fixed
11. demoRoutes.ts sync summary
12. Code hygiene cleanup performed
13. Demo data hygiene observations
14. Backend readiness observations
15. Files modified
16. Build result
17. Remaining risks
18. Recommended next prompt

------------------------------------------------

PASS/FAIL STANDARD

This pass is successful if:

- Demo portals open without real Supabase auth
- Locked Veroxa pricing is consistent
- Each role has clean navigation
- Internal pages are not exposed awkwardly
- Primary role homepages are clear
- Client portal feels simpler
- Owner portal is not operationally noisy
- Operator portal is oversight-focused
- Team portal is execution-focused
- demoRoutes.ts is more honest
- Build passes

Do not add new features.

This is the cleanup that prepares Veroxa for the next stage.