VEROXA GIGANTIC FULL POWER PROMPT — MIGRATION 001/002 REVIEW + MIGRATION 003 DRAFT + MIGRATION 004 PLAN

Objective:
Continue Veroxa backend preparation in a controlled, safe, documentation-first way.

This prompt combines the next 3 backend-prep building steps:

1. Review and correct Migration 001 + Migration 002 drafts
2. Create Migration 003 Media Foundation plan + draft-only SQL
3. Create Migration 004 Posting Foundation plan only

This is still NOT a live backend implementation.
This is still NOT to be run.
This is still NOT a Supabase activation pass.

------------------------------------------------

ABSOLUTE RULES

Do NOT run migrations.
Do NOT move files into supabase/migrations.
Do NOT connect Supabase.
Do NOT activate real auth.
Do NOT change AUTH_MODE.
Do NOT change auth guards.
Do NOT change portal UI.
Do NOT change pricing.
Do NOT change navigation.
Do NOT build uploads.
Do NOT add AI APIs.
Do NOT build publishing.
Do NOT add payment processing.
Do NOT change visible app behavior.
Do NOT change routes.
Do NOT edit portal pages.

Keep AUTH_MODE as:

"placeholder"

Keep pricing unchanged:

Google Presence Starter:
$497/mo

Complete Online Presence:
12-month: $997/mo
6-month: $1,097/mo
3-month: $1,197/mo
No-contract: $1,497/mo

------------------------------------------------

SOURCE FILES TO READ FIRST

Read and align with:

docs/SUPABASE_SCHEMA_DRAFT_V1.md
docs/SUPABASE_RLS_PLAN_V1.md
docs/DEMO_DATA_MAP.md
docs/MIGRATION_001_TEST_PLAN.md
docs/MIGRATION_002_CLIENT_FOUNDATION_PLAN.md
docs/MIGRATION_002_TEST_PLAN.md if it exists
docs/sql_drafts/001_veroxa_schema_draft.sql
docs/sql_drafts/002_rls_policy_examples.sql
docs/sql_drafts/migrations_review/001_identity_foundation_draft.sql
docs/sql_drafts/migrations_review/002_client_foundation_draft.sql if it exists

Also inspect:

src/data/demo/
src/lib/auth/authMode.ts

Before editing, confirm:
- AUTH_MODE is still placeholder
- no real migration folder is being used
- draft files remain in docs/sql_drafts/migrations_review/

------------------------------------------------
PHASE 1 — REVIEW + CORRECT MIGRATION 001 AND 002 DRAFTS
------------------------------------------------

PART 1 — MIGRATION 001 REVIEW

Review:

docs/sql_drafts/migrations_review/001_identity_foundation_draft.sql
docs/MIGRATION_001_TEST_PLAN.md

Confirm Migration 001 includes only:

- user_profiles
- team_members
- set_updated_at()
- identity helper functions
- RLS for user_profiles and team_members
- basic indexes

Confirm Migration 001 does NOT include:

- clients
- team_client_assignments
- client_platforms
- onboarding_items
- client_requests
- media_assets
- posts
- post_slots
- reports
- notifications
- activity_logs
- AI tables
- storage
- pricing tables

If needed, correct comments, test coverage, helper safety, or RLS wording.

Do not add new tables to Migration 001.

------------------------------------------------

PART 2 — MIGRATION 001 SELF-UPDATE HARDENING

Make sure user self-update is clearly restricted.

Safe self-edit fields only:

- display_name
- avatar_url

Sensitive fields must remain owner-only:

- role
- client_id
- email
- is_active

If the draft uses a trigger guard, make sure the comments and test plan clearly explain it.

Add or verify tests:

- user can update own display_name
- user can update own avatar_url
- user cannot update own role
- user cannot update own client_id
- user cannot update own email
- user cannot update own is_active
- team/operator cannot update their own role
- owner can update another user’s role
- owner role change requires owner permissions

------------------------------------------------

PART 3 — MIGRATION 001 HELPER FUNCTION HARDENING

Review helper functions:

private.current_user_role()
private.current_user_client_id()
private.is_owner()
private.is_operator()
private.is_team_member()
private.is_system_actor()

Confirm:

- explicit private schema
- explicit search_path
- stable where appropriate
- security definer where justified
- no dynamic SQL
- no dependency on Migration 002 tables
- null/false fallback
- no broad access bug

If any helper is risky, correct the draft-only SQL and document why.

------------------------------------------------

PART 4 — MIGRATION 002 REVIEW

Review:

docs/MIGRATION_002_CLIENT_FOUNDATION_PLAN.md
docs/sql_drafts/migrations_review/002_client_foundation_draft.sql if it exists
docs/MIGRATION_002_TEST_PLAN.md if it exists

Migration 002 should include only:

- clients
- team_client_assignments
- client_platforms
- onboarding_items
- client_requests
- user_profiles.client_id FK addition
- client foundation helper functions
- RLS for those tables
- client-safe view stubs if planned

Migration 002 should NOT include:

- media_assets
- notifications
- activity_logs
- posts
- post_slots
- weekly_reports
- monthly_reports
- AI tables
- storage
- publishing
- payment systems

Correct any accidental overreach.

------------------------------------------------

PART 5 — MIGRATION 002 TABLE CHECK

Confirm the following are drafted correctly:

clients:
- timezone required
- no Toronto default
- demo seed may use America/Chicago
- pricing stored in monthly_fee_cents
- Google Presence Starter is service_package, not plan_type
- only Owner can change pricing fields

team_client_assignments:
- replaces assigned_client_ids uuid[]
- unique(team_member_id, client_id)
- assignment_role values:
  executor
  reviewer
  scheduler
  reporter
  lead

client_platforms:
- client-safe view must hide internal notes

onboarding_items:
- unique(client_id, item_key)

client_requests:
- client can create own requests only
- client cannot create requests for another client

------------------------------------------------

PART 6 — MIGRATION 002 HELPER + RLS CHECK

Confirm helpers are drafted or planned:

private.is_assigned_to_client(p_client uuid)
private.can_view_client(p_client uuid)
private.can_manage_client_operations(p_client uuid)
private.can_manage_pricing()

Rules:
- use team_client_assignments
- no assigned_client_ids array logic
- explicit search_path
- stable where appropriate
- false fallback
- no dynamic SQL

Confirm RLS planning:

clients:
- client own access only through client-safe views
- team assigned clients only
- operator all operational access
- owner full access including pricing

team_client_assignments:
- client no access
- team own active assignments
- operator select all
- owner full access

client_platforms:
- client-safe view required for client
- team/operator manage operationally
- owner full access

onboarding_items:
- client can update client-owned items only
- team/operator/owner broader access by role

client_requests:
- client own requests only
- team assigned requests
- operator/owner all

------------------------------------------------

PART 7 — MIGRATION 001 / 002 READINESS DECISION

At the end of Phase 1, add a short readiness note:

Migration 001:
- Ready after human review
- Needs correction
- Not ready

Migration 002:
- Ready after human review
- Needs correction
- Not ready

Be honest.

------------------------------------------------
PHASE 2 — MIGRATION 003 MEDIA FOUNDATION PLAN + DRAFT SQL
------------------------------------------------

PART 8 — CREATE MIGRATION 003 PLANNING DOCUMENT

Create:

docs/MIGRATION_003_MEDIA_FOUNDATION_PLAN.md

Status:
Planning only. No live migration. AUTH_MODE remains placeholder.

Purpose:
Migration 003 introduces the first operational layer after clients exist.

Migration 003 should include:

- media_assets
- notifications
- client_health_snapshots
- activity_logs

Do NOT include:

- posts
- post_slots
- weekly_reports
- monthly_reports
- content_concepts
- draft_sets
- draft_variants
- AI agents
- storage buckets
- publishing integrations

Explain dependency:
Migration 003 depends on Migration 001 and Migration 002.

------------------------------------------------

PART 9 — MIGRATION 003 TABLE PLAN: media_assets

Document and draft:

media_assets

Fields:

- id uuid primary key default gen_random_uuid()
- client_id uuid not null references clients(id) on delete cascade
- uploaded_by_user_id uuid references user_profiles(id) on delete set null
- file_url text not null
- thumbnail_url text
- file_type text not null check in ('image','video')
- mime_type text not null
- width_px integer
- height_px integer
- duration_seconds numeric
- source_type text not null default 'client_upload' check in ('client_upload','legacy_reuse','team_upload')
- title text
- caption_hint text
- client_safe_note text
- internal_note text
- quality_ai_flag text check in ('likely_usable','borderline','likely_reject')
- quality_score integer
- review_status text not null default 'uploaded' check in (
  'uploaded',
  'ai_reviewed',
  'team_review_pending',
  'rejected',
  'usable',
  'shortlisted',
  'drafted',
  'approved',
  'scheduled',
  'used',
  'reusable_archive'
)
- rejection_reason text
- reuse_eligible boolean not null default false
- linked_post_id uuid nullable
- uploaded_at timestamptz not null default now()
- created_at timestamptz not null default now()
- updated_at timestamptz not null default now()

Important:
- linked_post_id FK should be added later after posts table exists in Migration 004.
- Do not reference posts yet as active FK.
- Client-safe view must hide internal_note and raw rejection_reason.

------------------------------------------------

PART 10 — MIGRATION 003 TABLE PLAN: notifications

Document and draft:

notifications

Fields:

- id uuid primary key default gen_random_uuid()
- client_id uuid references clients(id) on delete cascade
- target_role text not null check in ('client','team','operator','owner')
- target_user_id uuid references user_profiles(id) on delete set null
- notification_type text not null check in ('success','info','warning','reminder','critical')
- priority text not null default 'p2' check in ('p1','p2','p3')
- title text not null
- message_body text not null
- status text not null default 'created' check in ('created','sent','seen','dismissed','escalated')
- trigger_source text not null default 'system' check in ('system','agent','operator','team','client_action')
- created_at timestamptz not null default now()
- updated_at timestamptz not null default now()

Rules:
- client can only see client-targeted notifications for their own client
- team can see assigned-client notifications relevant to team
- operator can see all operational alerts
- owner can see all

------------------------------------------------

PART 11 — MIGRATION 003 TABLE PLAN: client_health_snapshots

Document and draft:

client_health_snapshots

Fields:

- id uuid primary key default gen_random_uuid()
- client_id uuid not null references clients(id) on delete cascade
- level text not null check in ('healthy','attention','critical')
- priority_level text not null default 'normal' check in ('low','normal','high','critical')
- content_runway_days integer
- approved_media_count integer
- scheduled_posts_count integer
- open_requests_count integer
- unresolved_alerts_count integer
- summary text
- created_by_role text not null default 'system' check in ('system','operator','owner')
- created_at timestamptz not null default now()

Rules:
- client may see simplified health only through a future client-safe view
- operator/owner see full details
- system inserts snapshots

------------------------------------------------

PART 12 — MIGRATION 003 TABLE PLAN: activity_logs

Document and draft:

activity_logs

Fields:

- id uuid primary key default gen_random_uuid()
- client_id uuid references clients(id) on delete cascade
- entity_type text not null
- entity_id uuid
- action_key text not null
- description text
- performed_by_role text not null check in ('system','client','team','operator','owner')
- performed_by_user_id uuid references user_profiles(id) on delete set null
- old_value_json jsonb
- new_value_json jsonb
- created_at timestamptz not null default now()

Rules:
- append-only
- no client access to raw activity_logs
- team may view assigned-client operational logs only if needed
- operator/owner can view all
- system writes most logs
- pricing changes must create activity logs later

------------------------------------------------

PART 13 — MIGRATION 003 CLIENT-SAFE VIEWS

Plan or stub client-safe views:

client_portal_media_view:
- client_id
- id
- file_type
- thumbnail_url
- title
- caption_hint
- client_safe_note
- review_status translated into client-safe label
- uploaded_at

Do NOT expose:
- internal_note
- raw rejection_reason
- quality_score if too internal
- AI/system reasoning

client_portal_notifications_view:
- only target_role='client'
- own client only
- title
- message_body
- status
- created_at

client_portal_health_view:
- simplified health label
- content runway summary
- no internal risk logic

If actual view SQL is too early, create commented stubs.

------------------------------------------------

PART 14 — MIGRATION 003 RLS PLAN

Document and draft RLS rules:

media_assets:
- client select own through client-safe view
- client insert own with source_type pinned to client_upload and review_status pinned to uploaded
- client cannot update review_status, internal_note, quality fields, or rejection_reason
- team can review/update assigned client media
- operator can view/update all operationally
- owner full access
- system can update derived status

notifications:
- client select own client-targeted notifications
- client update own notification status to seen/dismissed
- team select assigned client team notifications
- operator all operational
- owner all

client_health_snapshots:
- client only simplified view
- team assigned summary
- operator/owner all
- system insert

activity_logs:
- no client raw access
- team assigned select if needed
- operator/owner select all
- system insert
- app/operator/owner insert as needed
- update/delete restricted or forbidden

------------------------------------------------

PART 15 — MIGRATION 003 INDEX PLAN

Draft indexes:

media_assets(client_id)
media_assets(review_status)
media_assets(uploaded_at)
media_assets(source_type)

notifications(client_id)
notifications(target_role)
notifications(target_user_id)
notifications(status)
notifications(priority)

client_health_snapshots(client_id)
client_health_snapshots(created_at)

activity_logs(client_id)
activity_logs(entity_type, entity_id)
activity_logs(created_at)
activity_logs(performed_by_user_id)

Do not over-index.

------------------------------------------------

PART 16 — MIGRATION 003 SEED STRATEGY

Plan seed mapping:

demoMediaAssets.ts -> media_assets
demoNotifications.ts -> notifications
demoClientHealth.ts -> client_health_snapshots
demoActivityLogs.ts -> activity_logs, but only dev seed if needed

Rules:
- dev-only
- idempotent
- stable IDs
- no real client data
- avoid production seeding

------------------------------------------------

PART 17 — MIGRATION 003 TEST PLAN

Create:

docs/MIGRATION_003_TEST_PLAN.md

Include tests:

- Migration 001 and 002 exist first
- client can insert own media
- client cannot insert media for another client
- client insert pins source_type to client_upload
- client insert pins review_status to uploaded
- client cannot edit review_status
- client cannot see internal_note
- client cannot see raw rejection_reason
- team can review assigned client media
- team cannot review unassigned client media
- operator can view all media
- system can insert health snapshots
- client sees only simplified health view
- client can see own client-targeted notifications
- client cannot see team/operator/owner notifications
- activity_logs raw table hidden from client
- owner can view all activity logs
- rollback drops policies before tables

------------------------------------------------

PART 18 — CREATE MIGRATION 003 DRAFT SQL FILE

Create:

docs/sql_drafts/migrations_review/003_media_foundation_draft.sql

Header:

DO NOT RUN — MIGRATION REVIEW DRAFT ONLY
This file is not active.
It is not in the Supabase migrations folder.
Review and audit before converting into a real migration.
AUTH_MODE remains "placeholder".

Include draft SQL for:

- media_assets
- notifications
- client_health_snapshots
- activity_logs
- updated_at triggers
- RLS enable statements
- policy drafts/placeholders
- client-safe view stubs or commented view SQL
- indexes

Do not run it.

------------------------------------------------

PART 19 — MIGRATION 003 SELF-AUDIT

After creating the plan and draft SQL, self-audit:

- file is not in supabase/migrations
- DO NOT RUN header exists
- no migration was run
- only Migration 003 tables included
- no posts/reports/AI/storage included
- client-safe views planned
- RLS rules documented
- storage bucket not created
- AUTH_MODE unchanged
- pricing unchanged
- navigation unchanged

Readiness decision:
- ready after human review
- needs correction
- not ready

------------------------------------------------
PHASE 3 — MIGRATION 004 POSTING FOUNDATION PLAN ONLY
------------------------------------------------

PART 20 — CREATE MIGRATION 004 PLANNING DOCUMENT

Create:

docs/MIGRATION_004_POSTING_FOUNDATION_PLAN.md

Status:
Planning only. No SQL draft yet unless clearly asked later.

Purpose:
Migration 004 introduces the scheduling/posting foundation after identity, clients, and media exist.

Migration 004 should include:

- posts
- post_slots

Do NOT include:

- weekly_reports
- monthly_reports
- content_concepts
- draft_sets
- draft_variants
- AI agents
- publishing integrations
- real platform APIs
- storage buckets

Explain dependency:
Migration 004 depends on Migration 001, 002, and 003.

------------------------------------------------

PART 21 — MIGRATION 004 TABLE PLAN: posts

Document table:

posts

Fields:

- id uuid primary key default gen_random_uuid()
- client_id uuid not null references clients(id) on delete cascade
- media_asset_id uuid references media_assets(id) on delete set null
- concept_id uuid nullable
- draft_variant_id uuid nullable
- platform_name text not null check in ('instagram','facebook','google_business','tiktok','other')
- content_type text not null check in ('photo','reel','carousel','story')
- title text
- caption_text text
- post_status text not null default 'planning' check in (
  'planning',
  'awaiting_content',
  'ready_for_review',
  'approved',
  'ready_to_schedule',
  'scheduled',
  'published',
  'failed',
  'reschedule_required',
  'archived'
)
- scheduled_for timestamptz
- published_at timestamptz
- publish_failure_reason text
- is_reuse_based boolean not null default false
- created_by_user_id uuid references user_profiles(id) on delete set null
- approved_by_user_id uuid references user_profiles(id) on delete set null
- created_at timestamptz not null default now()
- updated_at timestamptz not null default now()

Notes:
- concept_id and draft_variant_id are nullable placeholders until Migration 006
- no real publishing API yet
- published_at is manually/demo/system updated later

------------------------------------------------

PART 22 — MIGRATION 004 TABLE PLAN: post_slots

Document table:

post_slots

Fields:

- id uuid primary key default gen_random_uuid()
- client_id uuid not null references clients(id) on delete cascade
- platform_name text not null check in ('instagram','facebook','google_business','tiktok','other')
- slot_date date not null
- slot_time time not null
- timezone text not null
- status text not null default 'open' check in ('open','reserved','scheduled','completed','skipped')
- reserved_post_id uuid references posts(id) on delete set null
- created_at timestamptz not null default now()
- updated_at timestamptz not null default now()

Constraint recommendation:
unique(client_id, platform_name, slot_date, slot_time)

Notes:
- timezone required
- client scheduling preferences can be added later
- no background job yet

------------------------------------------------

PART 23 — MIGRATION 004 MEDIA LINKAGE PLAN

Plan FK addition:

media_assets.linked_post_id references posts(id) on delete set null

This was deferred from Migration 003 because posts did not exist yet.

Document it in Migration 004 plan.

------------------------------------------------

PART 24 — MIGRATION 004 CLIENT-SAFE CALENDAR VIEW

Plan view:

client_portal_calendar_view

Fields:
- client_id
- post_id
- platform_name
- content_type
- client_safe_title
- scheduled_for
- published_at
- status_label
- thumbnail_url if available

Do NOT expose:
- internal approval fields
- draft_variant_id
- concept_id
- publish failure internal notes unless client-safe
- team/operator notes

------------------------------------------------

PART 25 — MIGRATION 004 RLS PLAN

posts:
- client can view own scheduled/published client-safe posts through view
- team can create/update posts for assigned clients
- operator can view/update all operationally
- owner full access
- system can update published/failed status later

post_slots:
- client can view own client-safe calendar slots
- team can manage assigned client slots
- operator all operational
- owner full access

No real publishing permission yet.

------------------------------------------------

PART 26 — MIGRATION 004 SEED STRATEGY

Map:

demoPosts.ts -> posts
demoPostSlots.ts -> post_slots
demoMediaAssets.ts linkage -> media_assets.linked_post_id after posts exist

Rules:
- dev-only
- stable IDs
- idempotent
- no real client data
- no real platform publishing

------------------------------------------------

PART 27 — MIGRATION 004 TEST PLAN OUTLINE

Create:

docs/MIGRATION_004_TEST_PLAN_OUTLINE.md

Include tests:

- client sees only own calendar view
- client cannot see draft/concept IDs
- team can create posts for assigned clients
- team cannot create posts for unassigned clients
- operator can view all posts
- owner can view all posts
- post slot unique constraint works
- media linked_post_id added only after posts table exists
- scheduled posts do not publish automatically
- failed status does not trigger real API
- rollback drops FK before posts table

------------------------------------------------

PART 28 — MIGRATION 004 SELF-AUDIT

Confirm:

- no SQL draft created unless intentionally documented
- no migration run
- no publishing APIs added
- no background jobs added
- no app behavior changed
- AUTH_MODE unchanged
- pricing unchanged
- navigation unchanged

Readiness:
- Planning complete
- Needs more planning
- Not ready

------------------------------------------------
FINAL REPORT REQUIRED
------------------------------------------------

At completion, report:

1. Files created
2. Files modified
3. Migration 001/002 corrections made
4. Migration 001 readiness decision
5. Migration 002 readiness decision
6. Migration 003 plan created
7. Migration 003 draft SQL created
8. Migration 003 tables drafted
9. Migration 003 RLS summary
10. Migration 003 client-safe views summary
11. Migration 003 test plan created
12. Migration 003 readiness decision
13. Migration 004 plan created
14. Migration 004 tables planned
15. Migration 004 client-safe calendar view planned
16. Migration 004 test outline created
17. Confirmation no migrations were run
18. Confirmation no files placed in supabase/migrations
19. Confirmation AUTH_MODE unchanged
20. Confirmation pricing unchanged
21. Confirmation navigation unchanged
22. Remaining risks
23. Recommended next prompt

PASS STANDARD:

- No real migrations run
- No backend connected
- No app behavior changes
- Migration 001 and 002 drafts are safer
- Migration 003 has a draft-only SQL file in docs/sql_drafts/migrations_review/
- Migration 004 has a planning document only
- Client-safe views are planned
- RLS risks are documented
- AUTH_MODE remains placeholder