VEROXA PROMPT 3 — MIGRATION 001 REVIEW + READINESS AUDIT

Objective:
Audit the Migration 001 identity foundation draft before it becomes a real Supabase migration.

This is a REVIEW ONLY task.

Do NOT edit files unless there is a typo in documentation.
Do NOT run migrations.
Do NOT move files into supabase/migrations.
Do NOT connect Supabase.
Do NOT activate real auth.
Do NOT change AUTH_MODE.
Do NOT change portal UI.
Do NOT change pricing.
Do NOT change navigation.
Do NOT build uploads.
Do NOT build AI APIs.
Do NOT build publishing.

Keep AUTH_MODE as:
"placeholder"

------------------------------------------------

FILES TO REVIEW

Review:

docs/sql_drafts/migrations_review/001_identity_foundation_draft.sql
docs/MIGRATION_001_TEST_PLAN.md
docs/SUPABASE_SCHEMA_DRAFT_V1.md
docs/SUPABASE_RLS_PLAN_V1.md
docs/sql_drafts/002_rls_policy_examples.sql

------------------------------------------------

PART 1 — SAFETY CHECK

Confirm:

- Migration draft is not inside supabase/migrations
- Migration draft clearly says DO NOT RUN
- No real migration was executed
- AUTH_MODE is still placeholder
- No app behavior changed

------------------------------------------------

PART 2 — TABLE REVIEW

Review user_profiles:

Check:
- id references auth.users(id)
- role enum is limited to client/team/operator/owner
- client_id is nullable and documented as FK later
- no system role is stored in user_profiles
- email is unique
- is_active exists
- timestamps exist

Review team_members:

Check:
- user_profile_id references user_profiles(id)
- assigned_client_ids uuid[] is not used
- team_client_assignments is deferred to Migration 002
- is_active exists
- timestamps exist

------------------------------------------------

PART 3 — HELPER FUNCTION REVIEW

Review helper functions:

- current_user_role()
- current_user_client_id()
- is_owner()
- is_operator()
- is_team_member()
- is_system_actor()

Check:

- explicit schema
- security definer where appropriate
- explicit search_path
- stable where appropriate
- null/false fallback
- no dynamic SQL
- no accidental broad access
- no helper depends on clients table before Migration 002

------------------------------------------------

PART 4 — RLS POLICY REVIEW

Review policies for:

user_profiles
team_members

Check:

- anon gets no access
- users can select own profile
- owner can view/update profiles
- operator can view profiles but cannot change role/pricing/owner fields
- team user can view own team member row only
- owner can manage team members
- no public access exists
- no policy accidentally grants all authenticated users full access

------------------------------------------------

PART 5 — TEST PLAN REVIEW

Review MIGRATION_001_TEST_PLAN.md.

Confirm it covers:

- anon blocked
- client own profile only
- team own row only
- operator read-only visibility
- owner full management
- role change owner-only
- inactive user behavior
- service role/system behavior
- rollback expectation

Add missing test suggestions in the final report.

Do not implement tests yet.

------------------------------------------------

PART 6 — READINESS DECISION

Make a clear recommendation:

Option A:
Ready to convert into real Migration 001 after human review

Option B:
Needs correction pass before real migration

Option C:
Not ready

Explain why.

------------------------------------------------

FINAL REPORT REQUIRED

Return:

1. Safety status
2. Table design score
3. Helper function score
4. RLS policy score
5. Test plan score
6. Issues found
7. Required fixes before real migration
8. Whether Migration 001 can be converted later
9. Recommended next prompt

PASS STANDARD:
- Honest audit
- No implementation
- No migration run
- Clear go/no-go recommendation