VEROXA PROMPT — PORTAL-CONNECT PASS + M006 SQL DRAFT

Objective:
Fix the key backend-readiness blockers found in the M001–M006 audit before any real migration is run.

This is still draft/review work only.

Do NOT run migrations.
Do NOT move files into supabase/migrations.
Do NOT connect Supabase.
Do NOT activate real auth.
Do NOT change AUTH_MODE.
Do NOT issue real client credentials.
Do NOT change portal UI.
Do NOT change pricing.
Do NOT change navigation.
Do NOT connect AI APIs.
Do NOT connect publishing APIs.
Do NOT add payment processing.
Do NOT change visible app behavior.

Keep:

AUTH_MODE = "placeholder"

Locked pricing:
- Google Presence Starter: $497/mo
- Complete Online Presence 12-month: $997/mo
- 6-month: $1,097/mo
- 3-month: $1,197/mo
- No-contract: $1,497/mo

All SQL files created in this pass must remain under:

docs/sql_drafts/migrations_review/

Every SQL draft must include:

DO NOT RUN — MIGRATION REVIEW DRAFT ONLY
This file is not active.
It is not in the Supabase migrations folder.
Review and audit before converting into a real migration.
AUTH_MODE remains "placeholder".

------------------------------------------------
SOURCE FILES TO READ FIRST
------------------------------------------------

Read:

docs/SUPABASE_SCHEMA_DRAFT_V1.md
docs/SUPABASE_RLS_PLAN_V1.md
docs/DEMO_DATA_MAP.md

docs/sql_drafts/migrations_review/001_identity_foundation_draft.sql
docs/sql_drafts/migrations_review/002_client_foundation_draft.sql
docs/sql_drafts/migrations_review/003_media_foundation_draft.sql
docs/sql_drafts/migrations_review/004_posting_foundation_draft.sql
docs/sql_drafts/migrations_review/005_reporting_foundation_draft.sql

docs/MIGRATION_001_TEST_PLAN.md
docs/MIGRATION_002_TEST_PLAN.md
docs/MIGRATION_003_TEST_PLAN.md
docs/MIGRATION_004_TEST_PLAN.md
docs/MIGRATION_005_TEST_PLAN.md

docs/MIGRATION_006_CONTENT_AI_LAYER_PLAN.md
docs/MIGRATION_006_TEST_PLAN_OUTLINE.md

Also verify:

src/lib/auth/authMode.ts
src/components/auth/InternalDemoGuard.tsx

------------------------------------------------
PHASE 1 — CREATE PORTAL-CONNECT CLIENT-SAFE VIEWS DRAFT
------------------------------------------------

Create:

docs/sql_drafts/migrations_review/002_003_004_portal_connect_views_draft.sql

Purpose:
Materialize the client-safe views that were previously only stubs/comments in M002–M004.

This draft applies AFTER:

- M001 identity
- M002 client foundation
- M003 media foundation
- M004 posting foundation

Do NOT apply it.
Do NOT run it.
Do NOT place it in supabase/migrations.

Create or draft these views:

1. client_portal_clients_view
2. client_portal_platforms_view
3. client_portal_onboarding_view
4. client_portal_requests_view
5. client_portal_media_view
6. client_portal_notifications_view
7. client_portal_health_view
8. client_portal_calendar_view

------------------------------------------------
VIEW 1 — client_portal_clients_view
------------------------------------------------

Purpose:
Client-safe account view.

Expose only safe fields such as:

- id as client_id
- business_name
- primary_contact_name
- primary_contact_email
- primary_contact_phone
- cuisine_type
- website_url
- address
- plan_type only if intentionally client-facing
- service_package only if intentionally client-facing
- posting_frequency_weekly
- timezone
- account_status as safe_account_status
- onboarding_complete
- created_at

Do NOT expose:

- monthly_fee_cents
- assigned_operator_id
- assigned_team_label
- internal risk notes
- risk_status unless rewritten into safe wording
- content_health_status unless rewritten into safe wording
- contract internals unless intentionally client-facing

Use security_invoker = true where appropriate.

Client should only see their own client record through existing RLS/helper logic.

------------------------------------------------
VIEW 2 — client_portal_platforms_view
------------------------------------------------

Purpose:
Client-safe platform/account connection visibility.

Expose:

- client_id
- platform_name
- access_status
- username_or_handle
- last_verified_at
- updated_at

Do NOT expose:

- notes
- internal setup comments
- operator/team-only troubleshooting details

------------------------------------------------
VIEW 3 — client_portal_onboarding_view
------------------------------------------------

Purpose:
Client-safe onboarding progress.

Expose:

- client_id
- item_key
- item_label
- description if safe
- status
- owner_role if client-safe
- priority if useful
- completed_at
- updated_at

Do NOT expose:

- internal team/operator notes
- blocked reasons unless rewritten into client-safe language

------------------------------------------------
VIEW 4 — client_portal_requests_view
------------------------------------------------

Purpose:
Client-safe request tracking.

Expose:

- id
- client_id
- request_type
- title
- description
- status
- priority
- due_date
- created_at
- updated_at

Do NOT expose:

- assigned_to_role if too internal
- internal handling notes
- staff-only routing logic

------------------------------------------------
VIEW 5 — client_portal_media_view
------------------------------------------------

Purpose:
Client-safe uploaded media visibility.

Expose:

- id
- client_id
- file_type
- thumbnail_url
- title
- caption_hint
- client_safe_note
- uploaded_at
- review_status translated into a client-safe label if possible

Do NOT expose:

- internal_note
- raw rejection_reason
- quality_score
- quality_ai_flag if too internal
- team/operator review details
- AI/system reasoning

If using a status label mapping, include safe labels such as:

uploaded → Received
ai_reviewed / team_review_pending → Under Review
usable / shortlisted / approved → Ready for Content
scheduled → Scheduled
used → Used
reusable_archive → Saved for Future Use
rejected → Needs Replacement

------------------------------------------------
VIEW 6 — client_portal_notifications_view
------------------------------------------------

Purpose:
Client-safe notifications.

Expose only:

- id
- client_id
- title
- message_body
- notification_type
- priority
- status
- created_at
- updated_at

Filter:

target_role = 'client'

Do NOT expose:

- team/operator/owner notifications
- trigger_source if too internal
- target_user_id unless needed
- internal escalation logic

------------------------------------------------
VIEW 7 — client_portal_health_view
------------------------------------------------

Purpose:
Client-safe simplified health/status.

Expose:

- client_id
- simplified_health_label
- content_runway_days
- approved_media_count
- scheduled_posts_count
- summary rewritten into client-safe wording
- created_at

Do NOT expose:

- raw risk scoring
- internal priority_level if too operational
- unresolved_alerts_count if it creates panic
- operator/system-only reasoning

------------------------------------------------
VIEW 8 — client_portal_calendar_view
------------------------------------------------

Purpose:
Client-safe calendar visibility.

Expose:

- client_id
- post_id
- platform_name
- content_type
- client_safe_title
- scheduled_for
- published_at
- status_label
- thumbnail_url if available

Do NOT expose:

- concept_id
- draft_variant_id
- approved_by_user_id
- created_by_user_id
- raw publish_failure_reason
- internal post workflow states unless mapped safely

Status labels should be client-safe.

------------------------------------------------
PHASE 2 — NOTIFICATIONS COLUMN-WRITE TRIGGER DRAFT
------------------------------------------------

Update or create a draft fix in:

docs/sql_drafts/migrations_review/003_media_foundation_draft.sql

or create a dedicated correction draft:

docs/sql_drafts/migrations_review/003_notifications_status_guard_draft.sql

Preferred:
Create a dedicated correction draft if that is cleaner.

Purpose:
Fix the audit issue where client can UPDATE any column on their own notifications.

Draft a BEFORE UPDATE trigger/function that allows client-role users to update only:

- status

And only to:

- seen
- dismissed

Sensitive columns must not be client-changeable:

- title
- message_body
- notification_type
- priority
- trigger_source
- target_role
- target_user_id
- client_id

If a non-owner/non-operator/non-system client tries to change anything except status to seen/dismissed, raise an exception or reject.

Document clearly:
This is a draft-only guard.
It must be tested before real credentials are issued.

Update M003 test plan to include:

- client can mark own notification as seen
- client can dismiss own notification
- client cannot update title
- client cannot update message_body
- client cannot update priority
- client cannot update target_role
- client cannot update another client’s notification

------------------------------------------------
PHASE 3 — POST DELETE SLOT RESET TRIGGER DRAFT
------------------------------------------------

Update or create a draft fix in:

docs/sql_drafts/migrations_review/004_posting_foundation_draft.sql

or create dedicated correction draft:

docs/sql_drafts/migrations_review/004_post_slot_reset_guard_draft.sql

Purpose:
Fix the audit issue where deleting a post may leave a post_slot stuck as reserved with reserved_post_id = NULL.

Draft a BEFORE DELETE or AFTER DELETE trigger that:

- finds post_slots where reserved_post_id = OLD.id
- sets reserved_post_id = NULL
- sets status = 'open'
- updates updated_at

Document:
This is for operational cleanup only.
No real publishing is involved.

Update M004 test plan to include:

- deleting a post clears reserved_post_id
- deleting a post resets slot status to open
- deleting a post does not affect unrelated slots
- rollback drops this trigger before dropping posts

------------------------------------------------
PHASE 4 — WRITE M006 SQL DRAFT
------------------------------------------------

Create:

docs/sql_drafts/migrations_review/006_content_ai_layer_draft.sql

This is draft-only.

Include only:

- content_concepts
- draft_sets
- draft_variants
- ai_agents
- post FK additions:
  posts.concept_id → content_concepts(id)
  posts.draft_variant_id → draft_variants(id)
- updated_at triggers
- RLS draft policies
- indexes
- optional validation comments for additional_media_ids
- no real AI APIs

Do NOT include:

- OpenAI calls
- Anthropic calls
- model API keys
- background jobs
- publishing APIs
- payment tables
- financial snapshots

------------------------------------------------
M006 TABLE — content_concepts
------------------------------------------------

Draft:

content_concepts

Fields:

- id uuid primary key default gen_random_uuid()
- client_id uuid not null references clients(id) on delete cascade
- media_asset_id uuid references media_assets(id) on delete set null
- additional_media_ids uuid[] nullable
- content_angle text not null
- content_goal text check in ('awareness','engagement','conversion','branding','recovery')
- hook_style text check in ('question','bold_statement','story','stat','behind_scenes')
- cta_direction text check in ('visit','order','book','follow','share','none')
- status text not null default 'generated' check in ('generated','under_review','rejected','approved')
- generated_at timestamptz
- generated_by_agent text
- reviewed_by_user_id uuid references user_profiles(id) on delete set null
- created_at timestamptz not null default now()
- updated_at timestamptz not null default now()

Important:
- additional_media_ids uuid[] cannot be FK-enforced directly.
- Add comment that integrity is application-enforced or later trigger-validated.
- No client access in V1.

------------------------------------------------
M006 TABLE — draft_sets
------------------------------------------------

Draft:

draft_sets

Fields:

- id uuid primary key default gen_random_uuid()
- concept_id uuid not null references content_concepts(id) on delete cascade
- generation_version integer not null default 1
- status text not null default 'generated' check in ('generated','under_review','needs_regeneration','approved','archived')
- team_note text
- generated_at timestamptz
- generated_by_agent text
- created_at timestamptz not null default now()
- updated_at timestamptz not null default now()

No client access in V1.

------------------------------------------------
M006 TABLE — draft_variants
------------------------------------------------

Draft:

draft_variants

Fields:

- id uuid primary key default gen_random_uuid()
- draft_set_id uuid not null references draft_sets(id) on delete cascade
- variant_type text not null check in ('safe','engagement','sales')
- caption_body text not null
- hook_text text
- cta_text text
- hashtag_block text
- brand_voice_score integer
- status text not null default 'generated' check in ('generated','under_review','approved','archived','used')
- used_in_post_id uuid references posts(id) on delete set null
- created_at timestamptz not null default now()
- updated_at timestamptz not null default now()

No client access in V1.

------------------------------------------------
M006 TABLE — ai_agents
------------------------------------------------

Draft:

ai_agents

Fields:

- id uuid primary key default gen_random_uuid()
- agent_key text not null unique
- name text not null
- category text not null check in ('content','operations','intelligence','executive')
- purpose text not null
- is_enabled boolean not null default false
- mode text not null default 'demo' check in ('demo','rule_based','api_connected')
- config_json jsonb
- last_run_at timestamptz
- created_at timestamptz not null default now()
- updated_at timestamptz not null default now()

Rules:
- is_enabled defaults false
- config_json must never store secrets
- no API keys
- no model credentials
- owner controls enable/disable
- system may update last_run_at later

------------------------------------------------
M006 RLS DRAFT
------------------------------------------------

content_concepts:
- client no access
- team can manage assigned client concepts
- operator can view/manage all operationally
- owner full access
- system/AI can insert later through service role

draft_sets:
- client no access
- team can manage assigned client draft sets through concept → client relation
- operator can view/manage all
- owner full access
- system/AI can insert later

draft_variants:
- client no access
- team can manage assigned client variants through draft_set → concept → client relation
- operator can view/manage all
- owner full access
- system/AI can insert later

ai_agents:
- client no access
- team no direct access unless explicitly read-only later
- operator can view
- owner can insert/update/enable/disable
- system can update last_run_at only later

------------------------------------------------
M006 INDEXES
------------------------------------------------

Draft indexes:

content_concepts(client_id)
content_concepts(media_asset_id)
content_concepts(status)

draft_sets(concept_id)
draft_sets(status)

draft_variants(draft_set_id)
draft_variants(status)
draft_variants(used_in_post_id)

ai_agents(agent_key)
ai_agents(category)
ai_agents(is_enabled)

------------------------------------------------
M006 TEST PLAN
------------------------------------------------

Materialize:

docs/MIGRATION_006_TEST_PLAN.md

Use the existing outline but make it full, with sections for:

- client cannot select content_concepts
- client cannot select draft_sets
- client cannot select draft_variants
- client cannot select ai_agents
- team can manage concepts for assigned clients
- team cannot manage concepts for unassigned clients
- team can manage draft sets through assigned concept client
- operator can view all content workflow
- owner can enable/disable ai_agents
- non-owner cannot enable/disable ai_agents
- system/service behavior for last_run_at
- posts concept_id FK added safely
- posts draft_variant_id FK added safely
- rollback drops post FKs before content/draft tables
- no real AI call occurs

------------------------------------------------
PHASE 5 — PORTAL QUERY LINT PLAN
------------------------------------------------

Create:

docs/PORTAL_QUERY_SAFETY_PLAN.md

Purpose:
Document that when real Supabase connection begins, client portal code must query client-safe views, not sensitive base tables.

Include forbidden direct client portal queries:

- clients
- client_platforms
- media_assets
- posts
- post_slots
- weekly_reports
- monthly_reports
- content_concepts
- draft_sets
- draft_variants
- activity_logs

Allowed client portal views:

- client_portal_clients_view
- client_portal_platforms_view
- client_portal_onboarding_view
- client_portal_requests_view
- client_portal_media_view
- client_portal_notifications_view
- client_portal_health_view
- client_portal_calendar_view
- client_portal_weekly_reports_view
- client_portal_monthly_reports_view

Add a future CI/grep check idea:
client portal files should not directly query sensitive base table names.

Do not implement CI yet.

------------------------------------------------
FINAL SELF-AUDIT
------------------------------------------------

Confirm:

- no files placed in supabase/migrations
- no migrations run
- AUTH_MODE remains placeholder
- pricing unchanged
- navigation unchanged
- portal UI unchanged
- no Supabase connection activated
- no real auth activated
- no AI APIs connected
- no publishing APIs connected
- all new SQL drafts say DO NOT RUN
- M006 SQL draft exists
- M006 full test plan exists
- portal-connect views draft exists
- notification status guard draft exists
- post slot reset draft exists
- portal query safety plan exists

------------------------------------------------
FINAL REPORT REQUIRED
------------------------------------------------

Report:

1. Files created
2. Files modified
3. Portal-connect views drafted
4. Sensitive fields hidden by each view
5. Notifications status guard drafted
6. Post slot reset trigger drafted
7. M006 SQL draft created
8. M006 test plan created
9. Portal query safety plan created
10. Confirmation no migrations were run
11. Confirmation no supabase/migrations files created
12. Confirmation AUTH_MODE unchanged
13. Confirmation pricing unchanged
14. Confirmation navigation unchanged
15. Remaining risks
16. Readiness recommendation:
   - Option A: Ready to apply M001 in dev after human review
   - Option B: Needs another correction pass
   - Option C: Not ready

PASS STANDARD:
- Critical blockers CB-1 and CB-2 are addressed in draft form
- No live backend change happens
- No auth/pricing/nav/UI drift
- Client-safe views are materialized in draft SQL
- M006 SQL is drafted but not run